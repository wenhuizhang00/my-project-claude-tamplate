# Vercel plugin (placeholder)

This directory holds **project-specific** Vercel-related extensions or scripts when you adopt the Vercel + Claude Code plugin workflow.

- Add official plugin artifacts or submodules per the [Vercel](https://vercel.com/docs) documentation for your integration.
- Do not commit **tokens** or **project IDs** that grant production access; use environment configuration instead.

Until a plugin is installed, this folder is intentionally minimal.
